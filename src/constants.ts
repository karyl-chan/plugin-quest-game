/**
 * Plugin identifier. Used as `manifest.plugin.id`, the URL prefix for
 * the WebUI (`/plugin/karyl-quest-game/...`), the capability namespace
 * (`plugin:karyl-quest-game:...`), and the bot-side service name.
 */
export const PLUGIN_KEY = "karyl-quest-game";
export const PLUGIN_NAME = "Karyl QuestGame";
export const PLUGIN_VERSION = "0.1.0";

/** Embed accent — QuestGame's classic green from the Python original. */
export const EMBED_COLOR = 0x73a936;
